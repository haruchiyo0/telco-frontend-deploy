[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

---

### 2. **Build Settings di Netlify Salah**

Pastikan konfigurasi build di Netlify Dashboard benar:

**Settings → Build & deploy → Build settings:**
- **Base directory**: `frontend`
- **Build command**: `npm run build`
- **Publish directory**: `frontend/dist`

---

### 3. **Environment Variables Belum Diset**

Tambahkan variabel environment di Netlify:

**Settings → Environment variables → Add variables:**
```
VITE_API_URL=https://telco-backend-api.onrender.com